'use strict';

function multiply(a, b, c, d) {
  return a * b * c * d;
}

const total = multiply(4, 35, 6, 90);
console.log('El total es ' + total);

//console.log('El total es ' + multiply(4, 35, 6, 90));
