'use strict';

const user = 'Corina';

if (user === 'Corina' || 'Ana') {
  console.log(`Bienvenida,  ${user} .`);
} else {
  console.log(
    'Lo siento pero el usuario que has introducido no está registrado.'
  );
}
